{
  "entry_version": "thin-1",
  "id": "us-atlas",
  "name": "us-atlas",
  "when_to_use": "需要美国州/县边界 choropleth 或全国轮廓时用它而非手写坐标,因为原生在真实 Census 几何、FIPS 标识和共享拓扑边界上失败。",
  "why_valuable": "AI 默认做不到可信的美国州县边界:坐标不可编造、县级 FIPS 与名称无法徒手配齐,共享弧段也不能靠生成文本保持一致;本包提供 Census 2017 边界的稳定 TopoJSON 地基。",
  "supply_form": "data-asset",
  "mode": "direct-import",
  "upstream": {
    "npm": "us-atlas",
    "version": "3.0.1",
    "github": "topojson/us-atlas"
  },
  "license": "ISC",
  "data_in": {
    "shape": "geometry",
    "note": "主结构词按 picks 取 geometry;fetch_url 的 counties-10m.json 是未投影、量化并简化的 TopoJSON,objects 精确含 counties、states、nation。县 id 是五位 FIPS,州 id 是两位 FIPS,properties.name 为名称。",
    "schema": {
      "type": "'Topology'",
      "objects": {
        "counties": {
          "type": "'GeometryCollection'",
          "geometries": [
            {
              "type": "'Polygon' | 'MultiPolygon'",
              "arcs": "number[][] | number[][][]",
              "id": "string —— 五位 county FIPS",
              "properties": {
                "name": "string"
              }
            }
          ]
        },
        "states": {
          "type": "'GeometryCollection'",
          "geometries": [
            {
              "type": "'Polygon' | 'MultiPolygon'",
              "arcs": "number[][] | number[][][]",
              "id": "string —— 两位 state FIPS",
              "properties": {
                "name": "string"
              }
            }
          ]
        },
        "nation": {
          "type": "'GeometryCollection'",
          "geometries": "单个 id='US' 的 Polygon | MultiPolygon"
        }
      },
      "arcs": "number[][][] —— TopoJSON delta-encoded shared arcs",
      "transform": {
        "scale": "[number, number]",
        "translate": "[number, number]"
      },
      "bbox?": "[number, number, number, number] —— 默认缺省"
    }
  },
  "fetch_url": "https://cdn.jsdelivr.net/npm/us-atlas@3.0.1/counties-10m.json",
  "artifact_note": "artifact 发布前应 fetch 此 pinned URL 并把 JSON 内联为常量;只有宿主明确白名单 cdn.jsdelivr.net 时才保留运行时 fetch。只需州界可换同版 states-10m.json;要免投影的 975×610 SVG/Canvas 坐标可换 counties-albers-10m.json,不要再套 d3 地理投影。",
  "isolation": {
    "shadowCompatible": "yes",
    "globalStyleInjection": "no",
    "globalDomMutation": "no",
    "globalEvents": "no",
    "evidence": "Static TopoJSON data-asset (counties-10m.json / states-10m.json / nation-10m.json + albers variants) — no runtime code, no exports, no DOM. Rendering is done entirely by the consumer's own d3-geo/topojson code, so it is inert with respect to shadow isolation.",
    "confidence": "high",
    "audited": "2026-07-23"
  }
}
