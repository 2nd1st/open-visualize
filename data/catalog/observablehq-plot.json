{
  "entry_version": "thin-1",
  "id": "observablehq-plot",
  "name": "Observable Plot",
  "when_to_use": "需要对表格数据做分箱、堆叠、分面小倍数或多 mark 分层 EDA 时用它而非手写 SVG,因为原生手写在聚合正确性、分面布局和共享尺度上失败;静态单一柱线饼非做它。",
  "why_valuable": "AI 默认做不到正确的分箱聚合、多分面尺度对齐和多 mark 组合,只能仿出单面静态图;Observable Plot 把这些高错率步骤收进 grammar-of-graphics 原语。",
  "supply_form": "component",
  "level": "L1",
  "mode": "direct-import",
  "upstream": {
    "npm": "@observablehq/plot",
    "version": "0.6.17",
    "github": "observablehq/plot"
  },
  "license": "ISC",
  "data_in": {
    "shape": "tabular",
    "note": "rows 是原始表格;marks 必须是 Plot.barY/lineY/dot/rectY/cell 等原生工厂返回的 Plot.Mark 运行时对象,不是字符串或自造 JSON DSL。",
    "schema": {
      "rows": "Record<string, string | number | boolean | Date | null>[]",
      "marks": "Plot.Mark[] —— 每个 mark 工厂直接消费 rows",
      "options?": {
        "_default": "{};width/height 由 boot 注入",
        "width?": "number —— boot 默认取容器像素宽,量不到时 640",
        "height?": "number —— boot 默认 400",
        "margin?": "number —— 默认由 Plot 自动推断",
        "x?": "Plot.ScaleOptions —— 默认从 mark 通道推断",
        "y?": "Plot.ScaleOptions —— 默认从 mark 通道推断",
        "color?": "Plot.ScaleOptions —— 默认从 mark 通道推断",
        "caption?": "string —— 默认无 caption"
      }
    }
  },
  "spec": {
    "height": 400,
    "note": "Plot.plot 产出 SVG/figure,boot 显式给默认 height 400px并量取容器 width;它没有原地重排 API,容器尺寸变化时应重建节点,铁律 3 禁 vh/百分比高。"
  },
  "boot": {
    "artifact": "// inferred-not-tested:artifact 列 H0 探针尚未闭合\nimport * as Plot from 'https://cdn.jsdelivr.net/npm/@observablehq/plot@0/+esm';\nconst el = document.getElementById('plot');\nconst options = { width: el.clientWidth || 640, height: 400, ...(data.options ?? {}), marks: data.marks };\nconst chart = Plot.plot(options);\nel.replaceChildren(chart);",
    "widget": "import * as Plot from 'https://cdn.jsdelivr.net/npm/@observablehq/plot@0/+esm';\nconst el = document.createElement('div'); shadowRoot.appendChild(el);\nconst options = { width: el.clientWidth || 640, height: 400, ...(data.options ?? {}), marks: data.marks };\nconst chart = Plot.plot(options);\nel.replaceChildren(chart);",
    "codex": "import * as Plot from 'https://cdn.jsdelivr.net/npm/@observablehq/plot@0/+esm';\nconst el = document.getElementById('plot');\nconst options = { width: el.clientWidth || 640, height: 400, ...(data.options ?? {}), marks: data.marks };\nconst chart = Plot.plot(options);\nel.replaceChildren(chart);"
  },
  "example": {
    "_why": "Plot 的数据是 JSON,但 marks 是原生工厂产物;用最小 JS 明确二者边界,避免把 mark 调用误塞成字符串。",
    "minimal": {
      "native_js": "const rows = [{category:'A',value:12},{category:'B',value:19}];\nconst data = { rows, marks: [Plot.barY(rows, {x:'category', y:'value'}), Plot.ruleY([0])] };"
    },
    "empty": {
      "native_js": "const rows = [];\nconst data = { rows, marks: [] };"
    }
  },
  "carriers": {
    "artifact": "inferred-not-tested",
    "widget": "supported:返回的 SVG/figure 直接落 shadow 容器,无必需全局 CSS 或上行能力",
    "codex": "supported:ESM CDN + SVG,无上行依赖"
  }
}
