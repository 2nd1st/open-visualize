{
  "entry_version": "thin-1",
  "id": "katex",
  "name": "KaTeX",
  "when_to_use": "需要把 LaTeX 快速排成稳定的行内或块级公式时用它,而非手写 HTML/SVG 字形——原生手写在分式、根式、上下标基线和伸缩定界符上失败。",
  "why_valuable": "AI 默认只能回显 LaTeX 源码或拼出脆弱的 Unicode 近似,做不到一致的数学间距、基线、伸缩括号与可访问 MathML；KaTeX 用一次 render 闭环这些排版细节。",
  "i18n": {
    "en": {
      "when_to_use": "Use it when you need to typeset LaTeX quickly as stable inline or display math instead of hand-writing HTML/SVG glyphs. Native implementations fail on fractions, radicals, subscript and superscript baselines, and stretchable delimiters.",
      "why_valuable": "AI can only echo LaTeX source or assemble fragile Unicode approximations by default. It cannot provide consistent mathematical spacing, baselines, stretchable brackets, and accessible MathML. KaTeX closes these layout details with one render call.",
      "data_in": {
        "schema": {
          "latex": "string - one LaTeX formula supported by KaTeX, without outer $ or $$",
          "displayMode?": "boolean - true for display math, false for inline math; default false",
          "throwOnError?": "boolean - whether to throw on parse failure; entry default false, displaying the source in an error color",
          "output?": "'htmlAndMathml' | 'html' | 'mathml' - default 'htmlAndMathml'",
          "trust?": "boolean - whether to allow trusted commands; default false",
          "macros?": "Record<string,string> - custom macro table; default {}"
        }
      }
    }
  },
  "supply_form": "component",
  "level": "L1",
  "mode": "direct-import",
  "upstream": {
    "npm": "katex",
    "version": "0.18.1",
    "github": "KaTeX/KaTeX"
  },
  "license": "MIT",
  "data_in": {
    "shape": "notation-string",
    "_why_no_example": "LaTeX 是 AI 原生记法,schema 足矣；不附 minimal example,避免把某条公式误当固定内容。",
    "schema": {
      "latex": "string —— 单条 KaTeX 支持的 LaTeX 公式,不含外层 $ 或 $$",
      "displayMode?": "boolean —— true 为块级公式,false 为行内公式；默认 false",
      "throwOnError?": "boolean —— 解析失败是否抛错；entry 默认 false,以错误颜色回显源码",
      "output?": "'htmlAndMathml' | 'html' | 'mathml' —— 默认 'htmlAndMathml'",
      "trust?": "boolean —— 是否允许受信任命令；默认 false",
      "macros?": "Record<string,string> —— 自定义宏表；默认 {}"
    }
  },
  "boot": {
    "artifact": "// KaTeX CSS 使用已发布的 pinned 资产；字体被 CSP 拦截时公式结构仍可见\nimport katex from 'https://cdn.jsdelivr.net/npm/katex@0.18.1/+esm';\nconst css = document.createElement('link'); css.rel = 'stylesheet'; css.href = 'https://cdn.jsdelivr.net/npm/katex@0.18.1/dist/katex.min.css'; document.head.appendChild(css);\nconst el = document.getElementById('math');\nkatex.render(data.latex, el, { displayMode: data.displayMode ?? false, throwOnError: data.throwOnError ?? false,\n  output: data.output ?? 'htmlAndMathml', trust: data.trust ?? false, macros: data.macros ?? {} });",
    "widget": "// CSS 进 shadow；先把相对字体 URL 改成 pinned 绝对 URL\nimport katex from 'https://cdn.jsdelivr.net/npm/katex@0/+esm';\nconst base = 'https://cdn.jsdelivr.net/npm/katex@0.18.1/dist/';\nconst css = (await fetch(base + 'katex.min.css').then(r => r.text())).replaceAll('url(fonts/', `url(${base}fonts/`);\nconst style = document.createElement('style'); style.textContent = css; shadowRoot.appendChild(style);\nconst el = document.createElement('span'); shadowRoot.appendChild(el);\nkatex.render(data.latex, el, { displayMode: data.displayMode ?? false, throwOnError: data.throwOnError ?? false, output: data.output ?? 'htmlAndMathml', trust: data.trust ?? false, macros: data.macros ?? {} });",
    "codex": "import katex from 'https://cdn.jsdelivr.net/npm/katex@0.18.1/+esm';\nconst css = document.createElement('link'); css.rel = 'stylesheet'; css.href = 'https://cdn.jsdelivr.net/npm/katex@0.18.1/dist/katex.min.css'; document.head.appendChild(css);\nconst el = document.getElementById('math');\nkatex.render(data.latex, el, { displayMode: data.displayMode ?? false, throwOnError: data.throwOnError ?? false, output: data.output ?? 'htmlAndMathml', trust: data.trust ?? false, macros: data.macros ?? {} });"
  },
  "carriers": {
    "artifact": "inferred-not-tested:仅在打包阶段把 KaTeX webfont 转成 data: URI 后可用；不可把 jsDelivr font URL 直接留给 artifact",
    "widget": "conditional:katex.min.css 必须放 shadowRoot,且需允许 pinned jsDelivr woff2；不允许字体 CDN 时也要内联 data: URI",
    "codex": "conditional:ESM + CSS 可用；字体 CSP 严格时须走同一内联字体打包步骤"
  },
  "isolation": {
    "globalStyleInjection": "no",
    "globalDomMutation": "no",
    "globalEvents": "no",
    "shadowCompatible": "conditional",
    "evidence": "render(tex,element,options) 只写入传入 element,不注 katex.css(调用方供);无监听;conditional 因 webfonts 须到 shadow 作用域(钉 woff2 或 data: 内联)否则字形回退",
    "confidence": "high"
  },
  "example": {
    "minimal": {
      "latex": "x = \\frac{-b \\pm \\sqrt{b^2 - 4ac}}{2a}",
      "displayMode": true
    }
  }
}
