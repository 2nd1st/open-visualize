{
  "entry_version": "thin-1",
  "id": "driver-js",
  "name": "driver.js",
  "when_to_use": "需要在已渲染页面上做元素聚光、滚动定位与分步讲解时用它而非手写遮罩,因为原生手写在挖洞几何、浮层避让和跨步骤焦点管理上失败。",
  "why_valuable": "AI 默认只能写编号说明列表;做不到随目标尺寸变化的 spotlight 挖洞、popover 自动避让、滚动定位和键盘步进状态机。",
  "supply_form": "component",
  "level": "L1",
  "mode": "direct-import",
  "upstream": {
    "npm": "driver.js",
    "version": "1.8.0",
    "github": "kamranahmedse/driver.js"
  },
  "license": "MIT",
  "data_in": {
    "shape": "sequence",
    "note": "data_in 直接映射 driver({steps,...}) 的原生高频配置;element 字符串由上游用 document.querySelector 解析,所以目标必须在当前 document 中。",
    "schema": {
      "steps": [
        {
          "element?": "string CSS selector | Element —— 默认无目标,在页面中央显示浮层",
          "popover?": {
            "_default": "未提供时不显示 popover,仅聚光目标",
            "title?": "string —— 默认不显示标题",
            "description?": "string —— 默认不显示正文",
            "side?": "'top'|'right'|'bottom'|'left'|'over' —— 默认 bottom",
            "align?": "'start'|'center'|'end' —— 默认 start"
          }
        }
      ],
      "showProgress?": "boolean —— 默认 false",
      "animate?": "boolean —— 默认 true",
      "smoothScroll?": "boolean —— 默认 false",
      "allowClose?": "boolean —— 默认 true"
    }
  },
  "boot": {
    "artifact": "// inferred-not-tested:overlay/popover 写入 document.body,CSS 进入 document.head\nimport { driver } from 'https://cdn.jsdelivr.net/npm/driver.js@1/+esm';\nconst css = Object.assign(document.createElement('link'), { rel: 'stylesheet', href: 'https://cdn.jsdelivr.net/npm/driver.js@1/dist/driver.css' });\ndocument.head.appendChild(css);\nconst tour = driver({ animate: data.animate ?? true, showProgress: data.showProgress ?? false, smoothScroll: data.smoothScroll ?? false, allowClose: data.allowClose ?? true, steps: data.steps });\ntour.drive();",
    "widget": "unsupported:driver.js v1.8.0 把 overlay 与 popover 固定写入 document.body,且 element 字符串固定走 document.querySelector;上游无 shadow 容器参数,无法满足 widget 零足迹。",
    "codex": "// sandbox iframe:全局 DOM 写入被限制在当前 iframe\nimport { driver } from 'https://cdn.jsdelivr.net/npm/driver.js@1/+esm';\nconst css = Object.assign(document.createElement('link'), { rel: 'stylesheet', href: 'https://cdn.jsdelivr.net/npm/driver.js@1/dist/driver.css' });\ndocument.head.appendChild(css);\nconst tour = driver({ animate: data.animate ?? true, showProgress: data.showProgress ?? false, smoothScroll: data.smoothScroll ?? false, allowClose: data.allowClose ?? true, steps: data.steps });\ntour.drive();"
  },
  "example": {
    "_why": "step 的目标选择器与嵌套 popover 配置存在组合歧义,附最小原生配置。",
    "minimal": {
      "steps": [
        {
          "element": "#result",
          "popover": {
            "title": "结果区",
            "description": "先确认这里的汇总值。",
            "side": "bottom",
            "align": "start"
          }
        }
      ],
      "showProgress": true,
      "animate": true
    },
    "empty": {
      "steps": []
    }
  },
  "carriers": {
    "artifact": "inferred-not-tested",
    "widget": "unsupported:上游强制 document.body/document.querySelector 全局路径,不能把 overlay、popover 与目标查询限制在 shadowRoot",
    "codex": "supported:sandbox iframe 吸收 document.body 与 document.head 全局足迹;需同源 jsDelivr ESM 与 CSS"
  }
}
